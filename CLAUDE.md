# 자산지기 — 작업 규칙

단일 파일 PWA입니다. 앱 전체가 `index.html` 안에 있고(스타일·스크립트 인라인),
서버는 `apps-script/Code.gs`, 서비스워커는 `sw.js` 입니다.
GitHub Pages가 `main`을 그대로 서비스하므로 **브랜치에만 올리면 앱에는 반영되지 않습니다.**

## 고치기 전에 — 무조건

```
node tests/run.js
```

규칙 점검(`tests/invariants.js`)과 회귀 테스트가 한 번에 돕니다. **커밋 전에 반드시 통과해야 합니다.**

## 왜 규칙 점검이 있나

이 저장소에서 같은 종류의 결함이 여러 번 되돌아왔습니다. 원인은 늘 하나였습니다 —
**한 가지 판단을 여러 자리에서 하고 있는데, 고칠 때 한 곳만 고침.**
그래서 `tests/invariants.js`는 개별 증상이 아니라 **자리끼리 어긋난 상태 자체**를 잡습니다.
소스 모양 점검(R1~R7) + 행동 일치 점검(B1~B7)입니다.

새 규칙을 만들 때마다 여기에 한 줄, `invariants.js`에 검사 하나를 같이 넣으세요.

**검사를 넣었으면 반드시 '일부러 깨서' 잡히는지 확인하세요.** 안 그러면 통과만 하는
장식이 됩니다. 실제로 그런 일이 있었습니다 —
- 줄 수를 정해 두고 거슬러 오르던 `inside()` 가 함수가 길어지자 조용히 통과
- 이름을 좁게 잡은 R1 이 `(r.code||'').trim()===src` 를 못 봄
- B8 의 표본 데이터에 문제 상황(원화 계좌에 달러 코드)이 없어서 늘 통과
- 핸들러가 부르는 함수를 **모두 따라 들어가게** 했더니 어느 핸들러든 모든 낱말이 걸려,
  아무것도 못 잡는 검사가 됨(한 단계만, 그것도 `e.target` 을 넘기는 한 줄만 따라갑니다)
- 한 줄짜리 핸들러의 **다음 줄**을 본문으로 본 검사가 엉뚱한 핸들러를 보고 통과
- 닿지 않는 코드(화면에 없는 칸의 분기)에 검사를 붙여 두어 '덮여 있는 것처럼' 보임 → R13

**변수 이름이 같으면 검사도 사람도 구분할 수 없습니다.** 계좌 이름 편집의 `was` 와
리밸런싱 코드칸의 `wasCode` 를 갈라 둔 것이 그래서입니다.

## 지켜야 하는 규칙

| | 규칙 | 왜 |
|---|---|---|
| R1 | 종목코드끼리 비교는 **`mktKey()`** 를 거친다 | 대소문자·공백만 달라도 다른 종목이 되어, 보유량이 0 이 되고 "있는 만큼 또 사라"고 합니다 |
| R2 | 시장 대장(`S.mkts`)에 쓰는 것은 **`setCodeMarket()`** 뿐 | 여러 곳에서 쓰면 넘겨짚은 값이 확정값을 덮습니다 |
| R3 | 계좌 식별자는 **`acctKey()` / `snapAcctId()`** 로만 만든다 | 손으로 만들면 `id:undefined` 같은 열쇠로 여러 계좌가 뭉칩니다 |
| R4 | 추가 매수 후보는 **이름이 아니라 후보 열쇠**로 지목한다 | 이름이 같은 다른 종목이 있습니다(증권 상품 vs 코인) |
| R5 | `fillRow()` 는 **`pickable(cls)`** — 자산군까지 좁힌 후보에서만 | 이름만 맞추면 다른 자산군 종목의 코드·단가가 들어옵니다 |
| R6 | **편집이 끝나지 않은 값은 `S` 에 넣지 않는다** | `S` 에 넣으면 `persist()` 가 localStorage 에 적고, 앱이 죽으면 조각난 값이 확정된 것처럼 남습니다 |
| R7 | 옛 계좌에 id 를 처음 붙일 때는 **`seedAcctId()`**(이름에서 정해지는 값) | 무작위로 붙이면 기기마다 달라 같은 계좌의 기록이 둘로 갈라집니다 |
| R1b | `pickable()` 은 못 쓰는 코드를 **`codeFitsReb()`** 로 가려낸다 | 고르는 자리는 검증을 거치지 않습니다. 목록에서 **빼지는 말고** 코드·단가만 비웁니다 — 빼면 방식을 바꿀 때 그 행이 조용히 사라지고 다시 넣을 수도 없습니다 |
| R6b | 리밸런싱 **코드칸도** 편집 중에는 `S` 에 넣지 않는다 | 자동 저장이 '반쯤 적은 코드 + 앞 종목 단가'를 서버에 올립니다 |
| R6c | 마무리되지 않은 편집은 **`flushPendingEdits()`** 한 곳에서 확정 | 저장·이탈 경로가 갈라지면 한쪽만 고칩니다(계좌 이름만 고치고 코드칸을 빠뜨렸습니다) |
| R8 | 리밸런싱 행의 단가는 **`setRebPrice()`** 로만 넣는다 | 일곱 자리가 각각 즉석 판단을 하다가 손입력 한 곳을 빠뜨렸습니다. '가까이에 판단이 있나'를 보는 검사로는 모자랐습니다 — 판단은 그대로 두고 대입 조건에서만 빼도 통과했습니다. 그래서 **판단과 대입을 한 함수에 붙여** 두고, 그 함수를 거치지 않는 대입 자체를 금지합니다 |
| R8b | 입력 핸들러의 공용 대입(`r[f] = …`) 블록에는 **단가 분기가 살아 있어야** 한다 | 분기를 지우면 사람이 적은 값이 곧바로 `S` 에 들어갑니다. 호출만 보면 `if(false)` 로 바꿔 둔 것을 못 잡습니다 |
| R9 | 리밸런싱 행의 코드는 **`setRebCode()`** 로만 정한다 | 코드와 단가는 함께 움직여야 합니다. 자동매칭이 코드만 붙이고 손으로 적어 둔 옛 단가를 남겨, 그 단가로 매수량을 셈했습니다 |
| R10 | 입력칸을 새로 만드는 그리기는 **`withPendingEdits()`** 를 지난다 | 편집 중 값은 `S` 에 없으므로(R6) 그냥 다시 그리면 적던 이름·코드가 화면에서 사라집니다. 확정으로 때우면 조각난 값이 굳습니다. **지킬 함수를 이름으로 적어 두지 마세요** — 처음엔 두 개만 적어 두었다가 설정 화면을 빠뜨렸습니다. 검사는 '추적하는 칸이 든 틀을 `innerHTML` 로 새로 만드는 자리' 를 소스에서 찾습니다 |
| R10b | 다시 그리는 중(`painting`)에 오는 `change` 로는 **확정하지 않는다** | 브라우저는 입력칸이 화면에서 빠질 때 그 칸의 `change` 를 그 자리에서 띄웁니다. 그것을 확정으로 받아 '토스증' 이 이름으로 굳었고, 화면에는 확정된 값이 보여 알아채기도 어려웠습니다 |
| R11 | 계좌 종목의 단가는 **`setHoldPrice()`** 로만 넣는다 | 통화 판단이 시세 반영·자동매칭·이름으로 퍼뜨리기·손입력 네 곳에 흩어져 있었고, 앞의 세 곳이 통화를 안 봤습니다. 달러 단가가 원화 계좌에 들어가면 평가액과 시점 기록이 환율만큼 틀어집니다 |
| R11b | 보유종목 입력 핸들러의 공용 대입(`h[f] = num(…)`) 블록에도 **단가 분기가 살아 있어야** 한다 | R8b 와 같은 이유 |
| R10c | 편집칸은 **`focusout` 으로도 확정**한다 | 다시 그리며 되돌려 놓은 값은 코드가 넣은 것이라, 브라우저가 그 뒤의 blur 에 `change` 를 띄우지 않습니다. `change` 만 믿으면 다 적어 놓고 칸을 벗어났는데 화면에만 남고 `S` 에는 안 들어갑니다 |
| R12 | 불러온 상태는 **`loadState()`** 한 곳으로만 받아들인다 | 입력하는 길을 모두 막아도 '이미 저장되어 있던 값' 은 이 길로 들어옵니다(서버·옛 시점·파일·처음 켤 때). 받아들일 때 쓸 수 없는 단가를 버리고, 적던 값은 버립니다(그 상태의 것이 아닙니다) |
| R12b | 계좌 이름을 옮기면 **`retargetReb()`** 로 고른 계좌도 함께 옮긴다 | 이름을 옮기는 자리가 셋인데(이름 바꾸기·같은 이름 가르기·옛 시점 복원) 복원에서 빠뜨려, `rebAcct()` 가 첫 계좌로 넘어갔습니다 |
| R13 | 화면에 없는 칸을 다루는 **분기를 남겨 두지 않는다** | 닿지 않는 코드에 검사를 붙여 두면 덮여 있는 것처럼 보입니다 — 보유종목 표에서 코드 칸이 없어졌는데 그 분기·함수가 남아, 통화 검사를 거기에 붙이고 통과를 확인하고 있었습니다 |

### 판단은 한 함수에서만

| 판단 | 그 함수 | 다른 데서 하지 말 것 |
|---|---|---|
| 이 코드가 어느 시장인가 | `marketOfCode()` (대장 → `codeMarket()`) | 계좌 종류·자산군으로 추측 금지 |
| 이 코드를 이 계획에 쓸 수 있나 | `codeFitsReb()` → `marketFitsReb()` | 통화만 보거나 시장만 보는 즉석 판단 금지 |
| 이 종목을 어느 시장에서 찾나 | `rebMarket()` — **검색 전용** | "쓸 수 있나"를 겸하게 만들지 말 것 |
| 기록의 이 칸이 어느 계좌인가 | `snapAcctId()` | 이름 비교로 직접 찾지 말 것 |
| 이 행에 단가를 넣어도 되나 | `rebPriceOk(r)` (→ `codeFitsReb()`) | 자리마다 `r.code` 를 꺼내 따지지 말 것 — 일곱 자리 중 한 곳을 빠뜨립니다 |
| 이 행에 단가를 **넣는 것** | `setRebPrice(r, v[, from])` | `r.price = …` 를 직접 적지 말 것. 다른 종목에서 빌려오는 단가는 `from` 에 출처 코드를 넘길 것 |
| 이 행의 **코드를 정하는 것** | `setRebCode(r, code)` | `r.code = …` 를 직접 적지 말 것(비우는 것은 예외). 코드와 단가는 함께 움직입니다 |
| 이 계좌 종목에 단가를 넣어도 되나 | `holdPriceOk(cur, code)` | 자리마다 통화를 꺼내 견주지 말 것 |
| 계좌 종목에 단가를 **넣는 것** | `setHoldPrice(h, v, cur[, ps])` | `h.p = …` 를 직접 적지 말 것. 손입력은 `ps` 를 넘기지 않습니다(조회출처·사유를 건드리지 않으려고) |

### 값을 버려야 하는 때

단가·보유량·수량은 **"어느 종목/어느 계좌의 것인가"가 바뀌면 즉시 버립니다.**
- 코드가 바뀌면 → 단가 버림(시장이 같아도 다른 종목입니다)
- 종목을 바꾸면 → 수량 버림
- 계좌를 바꾸면 → 보유량 버림, 안 맞는 코드·단가 버림
- 통화가 안 맞으면 → 새 시세를 안 넣는 것으로 끝내지 말고 **들고 있던 값도** 버림

**단, 버리는 근거가 넘겨짚은 값이면 버리지 않습니다.** 시장을 알려 준 근거(시세 출처,
검색 결과)가 있을 때만 버립니다. `codeMarket()`·`codeMarketSure()` 는 알파벳 티커를 US 로
봅니다 — 코인 지갑에 `BTC` 라고 적은 사람의 단가를 그 추측으로 지우면 되돌릴 방법이 없습니다.
사람이 코드를 손으로 적는 자리에서는 **이미 있는 값을 버리지 않고**, 설정 화면의
'통화 안 맞음' 표시처럼 알리기만 합니다. 버리는 것은 시세 조회·검색 결과에 맡깁니다.

**'새 값을 안 받는 것' 과 '있던 값을 버리는 것' 은 다릅니다.** 안 받는 쪽(칸 잠금)은 넘겨짚은
값으로도 합니다 — 틀린 통화로 셈하느니 안 받는 편이 낫고, 코드를 고치면 바로 풀립니다.
대신 **빠져나갈 길을 함께 알려 주세요**(코드를 고치거나 수동으로 바꾸기).

## 리뷰 처리 절차

1. 지적 하나하나 **코드로 확인**하고, 실제 결함이면 고친다
2. 고친 것마다 **고치기 전/후 숫자**를 남기는 테스트를 `tests/` 에 추가
3. `node tests/run.js` — 규칙 점검 포함 전체 통과
4. **스스로 한 번 더 훑는다**(`/code-review`) — 리뷰가 찾기 전에 내가 찾는다
   + 화면을 거치는 규칙이면 `node tests/browser/pricegate.js` 도 돌린다
5. 같은 판단을 하는 **다른 자리**를 전부 확인한다 — 이게 반복의 원인이었다
6. 커밋 → 브랜치 + `main` 둘 다 푸시(Pages 가 `main` 을 봅니다)

## 브라우저 확인

껍데기(`tests/harness.js`)로는 **화면 이벤트가 돌지 않습니다** — 입력 핸들러를 통째로
지워도 `node tests/run.js` 는 통과합니다. 화면을 거치는 규칙은 진짜 브라우저에서 봅니다.

```
npm i playwright            # 저장소 밖에 깔아 쓰세요
node tests/browser/pricegate.js     # 단가를 넣어도 되는 자리인지
node tests/browser/draftkeep.js     # 다시 그려도 적던 값이 남는지 · 버려진 단가가 화면에도 반영되는지
```

**브라우저만 알려 주는 것이 실제로 있었습니다.** 입력칸이 화면에서 빠질 때 크로미움은 그 칸의
`change` 를 그 자리에서 띄웁니다(`paint 시작 → change(옛 칸) → paint 끝`). 그래서 적다 만
이름이 확정되어 굳었는데, 화면에는 확정된 값이 보여 눈으로는 알 수 없었습니다.
**리뷰 지적의 '원인 설명'도 그대로 믿지 말고 직접 재 보세요** — 이때 지적은 "change 가 오지
않아 사라진다" 였지만, 실제로는 change 가 와서 조각난 값이 굳고 있었습니다.

Chromium 은 `/opt/pw-browsers/chromium` 을 씁니다(`PW_CHROME` 로 바꿀 수 있습니다).
설치된 Playwright 가 다른 빌드를 찾으면 `executablePath` 로 이 경로를 직접 넘기세요.
Chart.js CDN 이 막히면 `npm i chart.js@4.4.1` 후 로컬 파일로 바꿔 띄웁니다.
PWA 는 완전히 닫았다 열어야 새 코드가 올라옵니다.

**페이지는 검사 프로세스가 직접 띄웁니다.** `npx http-server` 를 따로 띄웠다가 끝내도
죽지 않아, 다음 실행이 **앞서 띄운 옛 화면**을 보고 그대로 통과한 적이 있습니다 —
일부러 깨 놓고도 "잡힌다"고 잘못 읽을 뻔했습니다.
